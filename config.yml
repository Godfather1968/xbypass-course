title: XBypass
description: 'To learn to bypass WiFi encryption '
template:
  repo: xbypass-template
  name: xbypass
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
